{
  "name": "circuit-control-backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server/render-api.mjs",
    "start": "node server/render-api.mjs",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
